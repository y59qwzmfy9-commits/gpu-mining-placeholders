# 🚀 БЫСТРЫЙ СТАРТ — ДЕПЛОЙ FASTMINE БЕЗ БОЛИ

## ✨ САМЫЙ ПРОСТОЙ СПОСОБ (5 минут)

---

## 📦 ШАГ 1: ЭКСПОРТ ИЗ FIGMA MAKE

1. В Figma Make нажмите **Export** (правый верхний угол)
2. Выберите **Download ZIP**
3. Распакуйте архив в `~/Downloads/fastmine`

---

## 🔧 ШАГ 2: АВТОМАТИЧЕСКАЯ ПОДГОТОВКА

Откройте **Terminal.app** и выполните:

```bash
cd ~/Downloads/fastmine
bash prepare-deploy.sh
```

Скрипт автоматически:
- ✅ Заменит `App.tsx` на production-версию
- ✅ Заменит `FormatsImproved.tsx` на production-версию  
- ✅ Создаст папку `public/assets/`
- ✅ Проверит наличие изображений

---

## 📸 ШАГ 3: СКАЧАТЬ ИЗОБРАЖЕНИЯ

### **ВНИМАНИЕ: Это делается в CHROME, а не в Terminal!**

1. **Откройте Figma Make в Chrome**
2. **Откройте консоль:** `⌘ + ⌥ + J`
3. **Скопируйте скрипт** из файла `COPY_THIS_SCRIPT.txt`
4. **Вставьте** в консоль: `⌘ + V`
5. **Нажмите** Enter
6. Подождите 15-20 секунд

⚠️ **Если скрипт не работает:** откройте файл `DOWNLOAD_ALTERNATIVES.txt` — там 4 альтернативных метода!

### **Переместите изображения в проект:**

```bash
# В Terminal.app:
cd ~/Downloads/fastmine
mv ~/Downloads/*.png public/assets/
```

Или просто перетащите все PNG из Downloads в папку `public/assets/`

---

## 🚀 ШАГ 4: ДЕПЛОЙ НА VERCEL

### **Вариант A: Через CLI (быстро)**

```bash
# Установите Vercel CLI (один раз)
npm i -g vercel

# Перейдите в проект
cd ~/Downloads/fastmine

# Установите зависимости
npm install

# Деплой!
vercel
```

Следуйте инструкциям Vercel:
```
? Set up and deploy? → Y
? Link to existing project? → N  
? Project name? → fastmine
? In which directory? ��� ./
? Override settings? → N
```

**Готово!** 🎉 Vercel даст URL: `https://fastmine-xyz.vercel.app`

---

### **Вариант B: Drag & Drop (ещё проще!)**

1. Откройте [vercel.com](https://vercel.com)
2. Войдите в аккаунт
3. Нажмите **Add New** → **Project**
4. **Перетащите папку `fastmine/`** в окно браузера
5. Vercel автоматически задеплоит

**Готово!** 🎉

---

## ✅ ЧЕКЛИСТ:

```
□ Скачали ZIP из Figma Make
□ Распаковали архив
□ Запустили bash prepare-deploy.sh
□ Скачали 29 изображений (через Chrome консоль!)
□ Переместили PNG в public/assets/
□ Проверили: ls public/assets/*.png (29 файлов)
□ Задеплоили на Vercel
□ ГОТОВО! 🎉
```

---

## 🆘 ПОМОЩЬ:

### **Консоль не открывается:**
- Chrome: `⌘ + ⌥ + J`
- Safari: Включите меню "Разработка" в настройках, потом `⌘ + ⌥ + C`

### **Скрипт ��е запускается:**
```bash
chmod +x prepare-deploy.sh
bash prepare-deploy.sh
```

### **Изображения не скачиваются:**
- Используйте Chrome (не Safari!)
- Убедитесь что вы на странице Figma Make
- Разрешите множественные загрузки в настройках браузера

---

## 📞 ИТОГО:

1. **Export ZIP** из Figma Make
2. **bash prepare-deploy.sh** в Terminal
3. **⌘ + ⌥ + J** в Chrome → скачать 29 изображений
4. **mv ~/Downloads/*.png public/assets/** в Terminal
5. **vercel** в Terminal

**5 команд — и сайт онлайн!** 🚀

---

**Подробнее:** см. `EASY_DEPLOY_GUIDE.md`