# 📋 Как скопировать FormatsImproved.tsx

Файл **FormatsImproved.tsx** очень большой (~560 строк), поэтому его нужно скопировать вручную.

## 🎯 Способ 1: Из Figma Make (РЕКОМЕНДУЕТСЯ)

1. **В Figma Make найдите файл:**
   ```
   /FormatsImproved.production.tsx
   ```

2. **Откройте его** (кликните на имя файла в списке)

3. **Выделите весь код:**
   - macOS: `⌘ + A`
   - Windows: `Ctrl + A`

4. **Скопируйте:**
   - macOS: `⌘ + C`
   - Windows: `Ctrl + C`

5. **В Cursor создайте файл:**
   ```
   src/app/components/FormatsImproved.tsx
   ```

6. **Вставьте код:**
   - macOS: `⌘ + V`
   - Windows: `Ctrl + V`

7. **Сохраните:**
   - macOS: `⌘ + S`
   - Windows: `Ctrl + S`

✅ **ГОТОВО!**

---

## 🎯 Способ 2: Из скачанного ZIP

Если вы скачали код из Figma Make через Export:

1. Найдите файл `FormatsImproved.production.tsx` в скачанной папке
2. Откройте его в любом текстовом редакторе
3. Скопируйте всё содержимое
4. В Cursor создайте `src/app/components/FormatsImproved.tsx`
5. Вставьте скопированный код

---

## ⚠️ Важно!

**Используйте именно `FormatsImproved.production.tsx`, а НЕ `FormatsImproved.tsx`!**

- ❌ `/src/app/components/FormatsImproved.tsx` — содержит `figma:asset` импорты
- ✅ `/FormatsImproved.production.tsx` — готов к production с `/assets/` путями

---

## 🔍 Проверка

После копирования файл должен начинаться так:

```tsx
import React from "react";
import { Server, Flame, Sparkles, TrendingUp, Building2 } from "lucide-react";
import { ImageWithFallback } from "./figma/ImageWithFallback";

// Production paths for Vercel deployment
const fullCycleImage = "/assets/full-cycle.png";
const guaranteesImage = "/assets/guarantees.png";
...
```

Если видите `figma:asset` — вы скопировали не тот файл!

---

## 🆘 Если файл не находится

Возможно, вы ещё не экспортировали код из Figma Make.

**Решение:**

1. В Figma Make найдите кнопку **Export** (правый верхний угол)
2. Нажмите **Download ZIP**
3. Распакуйте архив
4. Найдите `/FormatsImproved.production.tsx`
5. Скопируйте как описано выше

---

## ✅ После копирования

Убедитесь что структура такая:

```
src/
└── app/
    ├── App.tsx
    └── components/
        └── FormatsImproved.tsx    ← Этот файл
```

Теперь можно запускать проект:

```bash
npm run dev
```
